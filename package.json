{
  "name": "orbiteus",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "admin-ui",
    "portal-ui",
    "packages/i18n"
  ],
  "scripts": {
    "build": "npm run build --workspace admin-ui",
    "dev": "npm run dev --workspace admin-ui",
    "test": "npm run test --workspaces --if-present"
  }
}
